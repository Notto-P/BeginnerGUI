@model IEnumerable<Webpayment.Models.db.Customers>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameCus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastnameCus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AddressCus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TelCus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameCus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastnameCus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AddressCus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TelCus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCus">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCus">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCus">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
